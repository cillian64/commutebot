* Copy config.example.py to config.py
* Edit config.py and fill in your API key and commute details
* Copy commutebot.example.service to commutebot.service
* Edit commutebot.service to match your username and the path to commutebot
* Run the following to install the service:
* `sudo cp commutebot.service /etc/systemd/system`
* `sudo chown root:root /etc/systemd/system/commutebot.service`
* `sudo systemctl enable commutebot`
* `sudo systemctl start commutebot`

